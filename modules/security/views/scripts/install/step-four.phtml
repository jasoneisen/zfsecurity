<h2>Step Four: Add Relation</h2>
<?php if (!empty($this->errors)): ?>
<p>The following error(s) occured:</p>
<ul class="error">
<?php foreach ($this->errors as $error): ?>
    <li class="error"><?php echo $error; ?></li>
<?php endforeach; ?>
</ul>
<?php endif; ?>
<p>Please add the following to your Base<?php echo $this->class; ?> class.
    If you plan on regenerating your models at any point, also add the
    relation to your schema file, whichever format it may be.</p>
<br />
<pre>
$this->hasMany('Group as Groups', array(
	'local'		=>	'<?php echo $this->column; ?>',
	'foreign'	=>	'group_id',
	'refClass'	=>	'SecurityGroupAccount'));
</pre>
<?php echo $this->form; ?>